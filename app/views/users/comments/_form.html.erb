        <%= form_with(model: [client, comment], url: client_comments_path(client), remote: true) do |f| %>
         <%= f.text_area :body, size: "60x5" %><br>
         <%= f.submit "保存",class: "btn btn-sm btn-outline-success" %>
        <% end %>